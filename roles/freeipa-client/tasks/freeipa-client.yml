---
- name: Enroll host in krb.vtluug.org domain
  command: >
    ipa-client-install
    -U
    --domain=krb.vtluug.org
    --server=chimera.private.vtluug.org
    --principal=admin
    --password="{{ freeipa_password }}"
  args:
    creates: /etc/ipa/default.conf
  when: inventory_hostname != 'chimera.private.vtluug.org' and "'nfs' not in group_names"
