---
- name: Install common packages for proxmox
  apt:
    name: "{{ item }}"
    state: latest
  with_items:
  - apt-listchanges
  - ca-certificates
  - libsasl2-2
  - libsasl2-modules
  - git
  - nmap
  - ntpstat
  - postfix
  - python-pip
  - tmux
  - unattended-upgrades
  - vim
  - zfs-zed

- name: Install required pip packages
  pip:
    name: "{{ item }}"
  with_items:
  - proxmoxer
  - requests

