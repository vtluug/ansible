---
- name: Install common packages
  apt:
    name: "{{ item }}"
    state: latest
  with_items:
  - apt-listchanges
  - ca-certificates
  - libsasl2-2
  - libsasl2-modules
  - git
  - nmap
  - ntpstat
  - postfix
  - tmux
  - unattended-upgrades
  - vim
  - zfs-zed
